/**
 * TODO(demo): Set to `false` before production to restore strict RBAC.
 *
 * When `true`, any authenticated user passes ADMIN role and permission checks
 * on the API (see RolesGuard and PermissionsGuard).
 */
export const DEMO_OPEN_ADMIN_ACCESS = true;

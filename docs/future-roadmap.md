# Future Roadmap

Product and engineering direction (informal). See `roadmap/` for structured tracking.

## Product

- Homepage builder fully backed by `HomepageSection` API
- Speaker follow and ratings in learner UI (schema exists)
- Optional revival of learning paths with migration plan

## Engineering

- Server Components where safe
- Split large admin CMS service
- API integration tests per module
- Rate limiting and CSP hardening

## Infrastructure

- Production S3, SMTP, web push, optional Elasticsearch at scale

Do not implement removed features without explicit product approval.

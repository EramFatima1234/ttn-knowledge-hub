# UI Guidelines

- Ant Design 6 APIs (`Alert` title, `Drawer` size)
- Aspire theme: `apps/web/src/styles/knowledgehub.scss`
- Reuse `AspireButton`, `AspireTable`, admin CMS components
- Three-column layout: sidebar, main, optional right panel
- Dynamic import for heavy widgets (AI, mini player, watch sections)

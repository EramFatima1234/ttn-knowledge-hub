# Coding Rules

From `.cursor/rules/coding-style.mdc`:

- Minimal diffs; match file conventions
- Strict TS; shared types in `packages/types`
- `kh-*` SCSS; no global header/aside selectors
- pnpm 9.15.4; Node ≥ 20
- Do not commit `.env` files

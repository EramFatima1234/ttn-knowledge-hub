# Troubleshooting

| Issue | Fix |
|-------|-----|
| Auth 500 | API up, OAuth client IDs match |
| CORS | `CORS_ORIGIN` |
| Empty data | `pnpm db:seed` |
| AI off | `GEMINI_API_KEY` on API |
| HMR Linux | `WATCHPACK_POLLING=true` |

`docs/debugging-guide.md`

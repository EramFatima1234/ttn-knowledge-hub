# KnowledgeHub Monorepo

See app-specific env files:
- `apps/web/.env.example`
- `apps/api/.env.example`

Quick start:

```bash
docker compose -f docker/docker-compose.yml up -d
cp apps/api/.env.example apps/api/.env
pnpm install
pnpm db:migrate
pnpm db:seed
pnpm dev
```

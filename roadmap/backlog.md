# Backlog

- Merged home feed API (`dashboard` + `recommendations`)
- Speaker follow + ratings learner UI
- Learning path revive **or** schema migration to drop tables
- Nest e2e tests per critical module
- API rate limiting
- Server Components spike on `/explore`

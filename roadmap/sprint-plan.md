# Sprint Plan (template)

Use for team planning — adjust dates per sprint.

## Sprint goals example

1. Documentation accuracy pass (API index, remove learning-path promises)
2. Homepage builder → `PUT /admin/cms/homepage` only
3. One admin CMS service extraction (meets submodule) — no behavior change

## Definition of done

- `pnpm lint` + `pnpm build` green
- Docs updated (`docs/api-contract.md`, relevant `architecture/` flow)
- Smoke: login, home, watch, one admin CMS path

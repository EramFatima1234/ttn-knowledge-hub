# Technical Debt

1. `admin-cms.service.ts` size (~1400 lines)
2. Stale learning-path references in legacy docs
3. `LearningPath` models without API
4. `Like` / `Rating` / `SpeakerFollow` schema without full API
5. Homepage builder localStorage vs `HomepageSection`
6. Client-heavy authenticated layout hydration cost
7. Committed Playwright report artifacts (optional gitignore)

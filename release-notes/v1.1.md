# Release Notes — v1.1

**July 2026** incremental release.

## Added

- Admin CMS URL/metadata fields (`homepageTags`, `displayPriority`)
- Recorded knowledge meet focus
- Gemini AI assistant (discover, summary, quiz)
- Performance optimizations (notifications lazy load, dynamic layout chunks, font subset)

## Changed

- Learning paths removed from active product surface (schema retained)

See `docs/performance-report.md` and `adr/ADR-006` through `ADR-010`.
